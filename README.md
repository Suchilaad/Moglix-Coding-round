# Moglix-Coding-round
Longest Valid Parentheses – Efficient C++ solution to find the length of the longest well-formed parentheses substring using a stack. Runs in O(n) time with O(n) auxiliary space.
